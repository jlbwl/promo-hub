# 富文本编辑器图文支持功能说明

## 功能概述

为渠道经理端的产品描述添加了完整的图文支持功能，支持电脑端和手机端的多种图片插入方式，以及智能的图片压缩处理。

## 功能特性

### 📱 电脑端功能
1. **拖拽上传**：直接将图片拖拽到编辑器中
2. **复制粘贴**：复制图片后直接粘贴到编辑器
3. **按钮上传**：点击工具栏"插入图片"按钮选择文件
4. **图片自适应**：图片自动适应编辑器宽度

### 📱 手机端功能
1. **按钮上传**：全屏宽度的上传按钮，方便操作
2. **智能压缩**：针对手机端优化的压缩参数
3. **响应式布局**：针对移动端优化的界面

### 🎨 图片处理功能
1. **智能压缩**：根据设备类型自动选择压缩参数
2. **质量优化**：保持清晰的同时减小文件体积
3. **尺寸限制**：自动调整图片到合适尺寸
4. **平滑绘制**：使用高画质绘制算法

### 🔒 安全与质量
1. **HTML清理**：粘贴内容时自动清理恶意代码
2. **文件验证**：检查文件类型和大小
3. **上传状态**：显示上传进度和状态

## 技术实现

### 压缩配置

| 设备类型 | 最大宽度 | 最大高度 | 压缩质量 | 最大文件大小 |
|---------|---------|---------|---------|-------------|
| 电脑端 | 1920px | 1920px | 0.85 | 10MB |
| 手机端 | 1080px | 1080px | 0.7 | 5MB |

### 组件文件
- **主组件**：[RichTextEditor.vue](/Users/sunkai/Documents/projects/self-promotion/apps/manager/src/components/RichTextEditor.vue)
- **使用位置**：[ProductEditView.vue](/Users/sunkai/Documents/projects/self-promotion/apps/manager/src/views/product/ProductEditView.vue)
- **API接口**：[/api/upload](/Users/sunkai/Documents/projects/self-promotion/apps/api/src/index.ts#L1677)

## 使用指南

### 1. 插入图片方式

#### 方式一：点击按钮
1. 点击编辑器工具栏中的"插入图片"按钮
2. 选择本地图片文件
3. 等待上传和压缩完成

#### 方式二：拖拽
1. 将图片文件从文件管理器中拖拽
2. 拖到编辑器区域
3. 松开鼠标完成上传

#### 方式三：粘贴
1. 复制图片（截图或图片文件）
2. 在编辑器中粘贴（Ctrl+V 或 Cmd+V）
3. 图片自动插入

### 2. 图片位置控制
- 将光标放在想要插入图片的位置
- 图片会自动在光标处插入
- 如果没有选中区域，图片会在文末插入

### 3. 编辑图文混排
- 图片可以前后添加文字
- 支持多图连续插入
- 每个图片独立一行显示

## 组件属性

```typescript
interface RichTextEditorProps {
  modelValue: string;           // 绑定值（HTML内容）
  placeholder?: string;         // 占位符文本
  maxLength?: number;          // 最大字符数（默认 5000）
}
```

## 暴露方法

```typescript
// 插入图片（外部调用）
insertImage(file: File): void

// 清空编辑器
clear(): void
```

## 样式特性

### 编辑器样式
- 最小高度：280px（电脑端）/ 200px（手机端）
- 最大高度：600px，超出滚动
- 柔和边框和阴影
- 聚焦高亮效果

### 图片样式
- 自动适应宽度
- 圆角边框
- 轻微阴影效果
- 悬停缩放动画

## 代码结构

```
RichTextEditor.vue
├── 模板 (Template)
│   ├── 编辑器区域
│   ├── 工具栏 (上传按钮、提示、字数统计)
│   └── 上传覆盖层
├── 脚本 (Script)
│   ├── 状态管理
│   ├── 压缩配置
│   ├── 图片处理流程
│   └── 事件处理
└── 样式 (Style)
    ├── 编辑器样式
    ├── 工具栏样式
    ├── 响应式适配
    └── 动画效果
```

## 主要改进点

### 1. 真实上传集成
- 调用 `/api/upload` 接口
- 使用 FormData 提交文件
- 处理上传成功和失败状态

### 2. 设备自适应
- 检测设备类型
- 自动应用对应压缩参数
- 响应式布局适配

### 3. 用户体验优化
- 实时字数统计
- 上传状态提示
- 友好的错误信息
- 平滑过渡动画

### 4. 安全增强
- HTML 内容清理
- 移除恶意脚本和样式
- 防止 XSS 攻击

### 5. 功能完整性
- 三种插入方式
- 完整的工具栏
- 加载状态指示
- 完善的错误处理

## 浏览器兼容性

- ✅ Chrome 60+
- ✅ Firefox 55+
- ✅ Safari 11+
- ✅ Edge 79+
- ✅ 移动端浏览器

## 注意事项

1. **文件大小**：建议单张图片在推荐范围内
2. **图片格式**：支持 JPG、PNG、GIF、WebP
3. **网络连接**：上传需要网络连接
4. **保存操作**：编辑完成后记得点击"保存"按钮

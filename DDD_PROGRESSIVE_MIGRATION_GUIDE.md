# Service 层到 DDD 架构渐进式迁移指南

## 一、核心原则

### 1.1 零停机原则
- 迁移过程中旧代码和新代码同时可用
- 不立即删除任何现有功能
- 利用 DDD 实现与现有代码桥接

### 1.2 逐步改造原则
- 从一个服务开始（建议从 UserService 开始）
- 改造完成后进行完整测试
- 确认无误后再继续下一个
- 逐步弃用旧代码

### 1.3 适配器模式原则
- 创建适配器层，让新架构实现旧接口
- 保持向后兼容性
- 降低迁移风险

## 二、整体架构设计

### 2.1 三层桥接架构

```
┌─────────────────────────────────────────────┐
│          Interface Layer (Controllers)      │
│              (逐步迁移)                      │
└────────────────┬────────────────────────────┘
                 │
┌────────────────▼────────────────────────────┐
│          ┌───────────────────────┐         │
│          │  Adapters Layer       │         │
│          │  (旧接口新实现)       │         │
│          │  - UserServiceAdapter │         │
│          │  - ProductService...  │         │
│          └───────────────────────┘         │
│                 ↓                          │
│  ┌────────────────────────────────────┐    │
│  │      Application Layer             │    │
│  │  (Commands/Queries/Handlers)       │    │
│  └────────────────────────────────────┘    │
│                 ↓                          │
│  ┌────────────────────────────────────┐    │
│  │         Domain Layer               │    │
│  │  (Aggregates/Value Objects/Events) │    │
│  └────────────────────────────────────┘    │
│                 ↓                          │
│  ┌────────────────────────────────────┐    │
│  │     Infrastructure Layer           │    │
│  │  (RepositoryImpl/Mappers)          │    │
│  └────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
```

## 三、迁移步骤分解

### 阶段一：准备阶段（不影响现有代码）
1. 创建共享 Kernel（Entity, ValueObject, AggregateRoot）
2. 定义 Repository 接口
3. 创建基础 Mapper 类

### 阶段二：Domain 层实现
1. 识别核心业务概念
2. 创建 Aggregate Root
3. 创建 Value Objects
4. 定义 Domain Events

### 阶段三：Infrastructure 层实现
1. Repository 实现
2. Mapper 实现
3. 与现有数据结构保持兼容

### 阶段四：Application 层实现
1. Command/Query 定义
2. Command/Query Handlers
3. 事务边界管理

### 阶段五：Adapter 层（关键）
1. 实现旧接口
2. 内部委托到新架构
3. 保持向后兼容

### 阶段六：测试与验证
1. 单元测试
2. 集成测试
3. 性能测试

### 阶段七：Controller 层迁移（可选）
1. 逐步替换 Controller 中的 Service 调用
2. 使用 Presenter 模式
3. 保持 API 接口不变

## 四、关键细节注意事项

### 4.1 数据格式兼容性
- 旧数据与新模型的映射
- 旧代码产生的数据要能被新代码读取
- 新代码产生的数据要能被旧代码读取

### 4.2 事务一致性
- 保持原有的事务行为
- 明确事务边界

### 4.3 缓存策略
- 新代码使用相同的缓存键
- 确保缓存失效逻辑一致

### 4.4 错误处理
- 保持相同的错误码
- 保持相同的错误信息结构
- 保持相同的 HTTP 状态码

### 4.5 日志记录
- 保持相同的日志格式
- 保持相同的日志级别

## 五、风险与缓解策略

### 5.1 风险列表
1. 数据不一致
2. 性能下降
3. 业务逻辑改变
4. API 接口改变

### 5.2 缓解措施
1. 完整的测试覆盖
2. 功能开关
3. 回滚机制
4. 灰度发布
